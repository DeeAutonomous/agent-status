# docs/

Drop a `screenshot.png` (PNG, ~1200px wide) or `screenshot.gif` here and the
top-level README will render it. Capture with `Cmd-Shift-5` → "Capture
Selected Window" of the menu bar with the dashboard popover open.
